import torch
import numpy as np

# torch.cuda.empty_cache()
# torch.cuda.get_device_name(0)
# torch.cuda.memory_summary(device=None, abbreviated=False)

prob = np.random.rand()# * 0.2
print(prob)